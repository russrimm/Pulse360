loglevel=error
ignore-workspace-root-check=true 
# Pin the public npm registry explicitly. A machine-level ~/.npmrc pointing at
# an internal Microsoft package proxy otherwise rewrites lockfile provenance
# (and downgrades integrity to sha1) on every install, breaking installs for
# outside contributors of this public repository.
registry=https://registry.npmjs.org/
