# RAI Audit Trail — Pulse 360°

Append-only, redacted evidence log of Rai's reviews. Never contains raw secrets or harmful content.

| Timestamp | Artifact | Verdict | Summary | Fix Agent |
|-----------|----------|---------|---------|-----------|
| 2026-06-20T11:38:29-05:00 | (init) | 🟢 | RAI policy seeded for Pulse 360°. No artifacts reviewed yet. | — |
| 2026-06-20T11:53:14-05:00 | Untrusted HTML render layer (8 sites) | 🔴 | **Critical Check #3 violated.** All 8 `dangerouslySetInnerHTML` render sites pass raw upstream feed HTML directly to the DOM. `isomorphic-dompurify` is installed but used only in `MessageCard.tsx` for text-preview extraction — not at any active render site. Affected files: `AzureUpdateCard.tsx` (update.description), `M365UpdateCard.tsx` (update.content), `ReleasePlanCard.tsx` (plan.businessValue), `ReleasePlanDetail.tsx` (plan.businessValue + plan.content), `MessageDetail.tsx` (processedContent — unsanitized message.content with injected h2 markup), `src/app/azure-update/[id]/page.tsx` (update.description), `src/app/m365-update/[id]/page.tsx` (update.content), `src/app/m365-update/rss/[id]/page.tsx` (update.content). Risk categories: script/event-handler injection, javascript: and data: URI injection, iframe/object embedding, CSS-based exfiltration, link-redirect trust abuse, misattribution of fabricated content as official Microsoft communication. No harmful payloads recorded; categories only. Recommended fix: server-side sanitization at ingestion in api.server.ts using isomorphic-dompurify with a strict tag/attribute/protocol allowlist and forced rel=noopener noreferrer on anchor elements. Additional note: MessageDetail.tsx processedContent transform injects h2 HTML after raw content — sanitization must occur AFTER this transform. | Alex (sanitization policy implementation) |
