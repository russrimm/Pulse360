# Session Log — Lint Restore (2026-06-20T15-31-22Z)

**Task:** Restore ESLint 9 flat-config linting  
**Agent:** Amos (Backend)  
**Duration:** 1 session  

## Outcome
✅ **Lint infrastructure restored** — `npm run lint` runs to completion.

## Key Findings
1. Root cause was dual: (a) no `.next/**` in ESLint ignores, (b) `pluginReact` had no file restriction, applied to JSON
2. JSON plugin's `SourceCode` lacks `getAllComments()` — react rules crash trying to call it
3. 257 pre-existing errors now visible (all in-scope for Russ triage, not Amos scope)
4. No source code changes; config-only fix to `eslint.config.mjs`

## Decision
Lint is restored to working state. Pre-existing errors deferred to Russ's remediation strategy.
